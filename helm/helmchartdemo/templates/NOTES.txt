
This file is generated by NOTES.txt. It's mostly to see what it's capable of...

{{ if .Values.dnsHostname }}
That's now deployed, you should be able to browse to either of the following URL's in the next 1-20 minutes:

- http{{ if .Values.tlsHosts }}s{{ end }}://{{ .Values.dnsHostname }}/
- http{{ if .Values.tlsHosts }}s{{ end }}://{{ .Values.dnsHostname }}/whoami

{{ else }}
That's now deployed, run the following command to get the AWS host name. You should be able to browse to in in the next 1-20 minutes:

    kubectl get ingress/ingress-whoami -n whoami-demo

{{ end }}